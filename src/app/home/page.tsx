import React from 'react';
import Shortener from '../../components/urlListing/Shortner';

const ShortenerPage: React.FC = () => {
    return (
        <Shortener />
    );
};

export default ShortenerPage;